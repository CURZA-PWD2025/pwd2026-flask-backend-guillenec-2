info:
  name: update_user
  type: http
  seq: 4

http:
  method: PUT
  url: "{{BACKEND_URL}}/users"
  body:
    type: json
    data: |-
      {
        "nombre":"admin",
        "email":"admin@update.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
