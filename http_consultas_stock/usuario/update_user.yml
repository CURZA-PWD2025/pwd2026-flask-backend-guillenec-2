info:
  name: update_user
  type: http
  seq: 4

http:
  method: PUT
  url: "{{BACKEND_URL}}/users/3"
  body:
    type: json
    data: |-
      {
        "email":"operador2@example.com",
        "nombre":"operador2"
      }
  auth:
    type: bearer
    token: "{{BEAR_TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
