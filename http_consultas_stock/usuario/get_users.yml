info:
  name: get_users
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:5001/api/v1/users
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
