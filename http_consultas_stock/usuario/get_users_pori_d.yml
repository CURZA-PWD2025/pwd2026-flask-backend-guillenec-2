info:
  name: get_users_pori_d
  type: http
  seq: 2

http:
  method: GET
  url: "{{BACKEND_URL}}/users/1"
  auth:
    type: bearer
    token: "{{BEAR_TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
