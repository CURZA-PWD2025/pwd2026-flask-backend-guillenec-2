info:
  name: get_categoria_id
  type: http
  seq: 2

http:
  method: GET
  url: "{{BACKEND_URL}}/categorias/1"
  auth:
    type: bearer
    token: "{{BEAR_TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
