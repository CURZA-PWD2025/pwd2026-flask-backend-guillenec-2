info:
  name: get_me
  type: http
  seq: 3

http:
  method: GET
  url: "{{BACKEND_URL}}/auth/me"
  auth:
    type: bearer
    token: "{{BEAR_TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
