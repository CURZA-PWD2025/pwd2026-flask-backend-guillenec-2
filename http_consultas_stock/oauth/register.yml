info:
  name: register
  type: http
  seq: 2

http:
  method: POST
  url: "{{BACKEND_URL}}/auth/register"
  params:
    - name: ""
      value: ""
      type: query
  body:
    type: json
    data: |-
      {
        "nombre":"admin",
        "email":"admin@example.com",
        "password":"admin123",
        "rol_id":1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
