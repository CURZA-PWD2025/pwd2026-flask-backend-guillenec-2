info:
  name: login
  type: http
  seq: 1

http:
  method: POST
  url: "{{BACKEND_URL}}/auth/login"
  body:
    type: json
    data: |-
      {
        "email": "admin@example.com",
        "password": "admin123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
