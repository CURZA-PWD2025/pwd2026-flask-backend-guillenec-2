info:
  name: delete_proveedores
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{BACKEND_URL}}/proveedores/3"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{BEAR_TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
