info:
  name: get_proveedores
  type: http
  seq: 1

http:
  method: GET
  url: "{{BACKEND_URL}}/proveedores"
  auth:
    type: bearer
    token: "{{BEAR_TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
