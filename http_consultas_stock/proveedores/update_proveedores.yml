info:
  name: update_proveedores
  type: http
  seq: 4

http:
  method: PUT
  url: "{{BACKEND_URL}}/proveedores/2"
  body:
    type: json
    data: |-
      {
        "nombre":"distribuidora roca 123",
        "contacto":"direccion 2",
        "telefono":"2944396887",
        "email":"rocaqgmail.com"
      }
  auth:
    type: bearer
    token: "{{BEAR_TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
