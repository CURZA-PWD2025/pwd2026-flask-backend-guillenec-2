info:
  name: crear_movimientos _entrada
  type: http
  seq: 3

http:
  method: POST
  url: "{{BACKEND_URL}}/movimientos"
  body:
    type: json
    data: |-
      {
        "tipo":"entrada",
        "cantidad":"10",
        "motivo":"Ingreso inicial de prueba",
        "producto_id":"2"
      }
  auth:
    type: bearer
    token: "{{BEAR_TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
