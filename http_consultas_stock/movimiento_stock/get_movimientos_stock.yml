info:
  name: get_movimientos_stock
  type: http
  seq: 1

http:
  method: GET
  url: "{{BACKEND_URL}}/movimientos"
  auth:
    type: bearer
    token: "{{BEAR_TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
