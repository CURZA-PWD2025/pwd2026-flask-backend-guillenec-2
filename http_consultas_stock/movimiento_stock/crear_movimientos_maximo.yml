info:
  name: crear_movimientos_maximo
  type: http
  seq: 5

http:
  method: POST
  url: "{{BACKEND_URL}}/movimientos"
  body:
    type: json
    data: |-
      {
        "tipo":"entrada",
        "cantidad":"1",
        "motivo":"Ingreso inicial de prueba",
        "producto_id":"2"
      }
  auth:
    type: bearer
    token: "{{BEAR_TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
