info:
  name: get_mis_movimientos
  type: http
  seq: 2

http:
  method: GET
  url: "{{BACKEND_URL}}/movimientos/mis"
  auth:
    type: bearer
    token: "{{BEAR_TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
