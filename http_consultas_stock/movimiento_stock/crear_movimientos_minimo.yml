info:
  name: crear_movimientos_minimo
  type: http
  seq: 4

http:
  method: POST
  url: "{{BACKEND_URL}}/movimientos"
  body:
    type: json
    data: |-
      {
        "tipo":"entrada",
        "cantidad":"10",
        "motivo":"Ingreso inicial de prueba",
        "producto_id":"1"
      }
  auth:
    type: bearer
    token: "{{BEAR_TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
