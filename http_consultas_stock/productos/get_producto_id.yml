info:
  name: get_producto_id
  type: http
  seq: 2

http:
  method: GET
  url: "{{BACKEND_URL}}/productos/3"
  auth:
    type: bearer
    token: "{{BEAR_TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
