info:
  name: get_productos
  type: http
  seq: 1

http:
  method: GET
  url: "{{BACKEND_URL}}/productos"
  auth:
    type: bearer
    token: "{{BEAR_TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
