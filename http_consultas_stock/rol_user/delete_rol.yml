info:
  name: delete_rol
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{BACKEND_URL}}/roles/rol_id"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
