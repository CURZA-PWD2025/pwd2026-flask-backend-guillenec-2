info:
  name: get_rol_id
  type: http
  seq: 2

http:
  method: GET
  url: "{{BACKEND_URL}}/roles/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
