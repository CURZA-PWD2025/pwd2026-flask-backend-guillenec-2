info:
  name: update_rol
  type: http
  seq: 4

http:
  method: GET
  url: "{{BACKEND_URL}}/roles/rol_id"
  body:
    type: json
    data: |-
      {
        "nombre":"user_update"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
